import { useParams } from "react-router-dom"

const Invoice = () => {

    const { id } = useParams();

    return (
        <div>
            <h1 ></h1>
        </div>
    )
}

export default Invoice